# 第三轮：证据到报告交付

2026-09-05。开发和确定性本地验收完成；真实模型整链路验收为**部分完成**，不得视为生产发布通过。未部署生产。

## 覆盖与结果

| 人工案例 | 已验证交付 | 边界 |
| --- | --- | --- |
| 定量 | CSV → 数据集 → NPS/均值 → Excel → Evidence → 大纲/脚本 | 当前定性渲染器明确拒绝定量图表 |
| 定性 | 两份访谈 → Segment/逐字原声 → Evidence → 大纲/脚本 → 可编辑 PPT | 长原声连续拆页，不改写 |
| 混合 | 上述两类来源 → 引用数值的定性 PPT + Excel | PPT 数值为引用文字，非原生定量图表 |

六组存储适配器×案例通过。JSON/文件存储和 SQLite/D1 适配层实际读写；另运行原生 workerd/D1/R2 回归。OfficeCLI 生成两份固定响应 PPT，Python 兼容渲染生成四份，六份文件均已写回对应测试存储并绑定来源。测试对象均为人工材料。

固定数值：16 行原始样本，NPS 有效 base 13，总体 NPS 84.6，VALUE 均值 6。校验引用字段，不能用样本基数冒充指标。原声可使用连续逐字摘录，记录起止位置；伪造后缀会被拒绝。每个续页片段拼接后必须与原声完全相同。

## 变更传播

- 成果保存证据和上游成果的内容指纹。排除证据、修改证据或同参数重新计算，会使相关大纲、脚本、PPT 显示“待更新”。历史导出也保留此状态。
- 旧内容和版本保留，沿用“基于此版本派生”入口生成新版本。标题修改不会误判为来源变化。
- 新增迁移 `0021_artifact_provenance.sql`。旧成果未伪造来源快照，有引用而缺少快照时显示“来源待复核”。部署时先完成迁移，再发布依赖该列的 API。
- 封面映射为章节引导布局；定量页面不会静默变成空白定性页。Service Worker 更新为 v78，只移除本应用旧缓存。

## 真实模型记录

配置模型 `glm-5.3-flash`，严格三次请求，每次最多 6,000 输出 token，总使用 **18,528 token**，没有隐藏重试。

1. 定性分析：达到输出上限，其中推理 5,673 token，未完成。失败保留。
2. 大纲：返回完整 JSON，但只有 2 条核心发现，未达至少 3 条质量门槛。本次验收提示仅传入原声 Evidence，遗漏既有分析的 insights/artifact summaries；生产处理器本身会传这些上下文。这是验收请求缺口，不能据此断言生产大纲链路失效。
3. 脚本：基于固定验收大纲生成 6 页，模型逐字摘录最初被整段相等校验误拒。修复后仅重放保存响应，原声和来源校验通过，真实 OfficeCLI 渲染通过，未再调用模型；该模型脚本的 PPT 也已存储并绑定来源，连同固定响应共有 7 份实际 PPT 输出。

因此尚未证明“真实模型定性分析 → 真实大纲 → 真实脚本”一次完整贯通。下一次专项验收应使用完整的生产上下文构建函数、控制推理预算并重新明确调用预算。本轮不自动扩大预算。

## 验证

- 全量 JavaScript 回归及 85 个单元测试通过；R3 新增六组交付、真实 HTTP 断流和真实 Chrome 缓存升级测试。
- 浏览器主套件 21 通过、1 跳过；新增“待更新”展示断言通过。
- HTTP 测试实际断开客户端连接，上游模型为模拟 DSH：已生成前缀持久化，后台完成，同一请求回放不重复生成。此结论不等同于真实远端模型断流验收。
- Python 定性 PPT 回归、Vite 构建、原生 workerd/D1/R2 通过。共检查 44 张实际导出截图，覆盖长原声、密集页、分群和续页。发现的 20 张脚注碰撞、标签重叠或裁切问题已修复，独立复查未发现残留关键问题。
- OfficeCLI 降级/不可用提示继续由现有服务与 UI 回归覆盖，生产渲染环境未发布。

可重跑：`npm run test:r3`；固定响应真实渲染及存储绑定：`npm run acceptance:r3:render`。Chrome 本地使用已安装浏览器，CI 安装 Chromium。真实模型脚本必须显式 `--run`；`--replay` 不联网，日志保留三次累计预算。不要删除预算记录后自动重试。

机器记录：[acceptance.json](validation/r3/acceptance.json)。原始模型大纲/脚本响应保留在同目录。真实 PPT 和 Excel 保留在验收索引所列 `.data` 测试路径。
