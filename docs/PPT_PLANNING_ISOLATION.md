# PPT 解析与结构预览隔离

2026-09-05：仅将 `/api/pptx-report/parse`、`/api/pptx-report/preview` 路由至独立进程（127.0.0.1:8002），复用原处理函数和参数。浏览器 `/pptx-api/*` 地址、解析算法、页面规划、快速/研究型报告分支均未修改。最终生成、渲染预览、AI 研究员和数据执行器保留原流程。

新服务 surveykit-ppt-planning 为单进程，最多 4 个并发连接、512 MiB 内存、1 核 CPU 上限，不启动渲染/AI 任务恢复，不获取重任务锁。解析/结构预览仍可能受上传网络、大文件复杂度和主机资源影响；研究型报告后续 AI 调用的时间不在本次优化范围。

验证：相同 XLSX 对原服务与隔离服务的解析、结构预览 JSON 完全一致；真实双进程测试阻塞渲染事件循环 8 秒期间，两接口仍完成。原接口错误行为和渲染路由隔离亦有检查。回滚时停止 planning 服务并将这两条 Nginx 路由恢复 8000，兼容旧部署包。
