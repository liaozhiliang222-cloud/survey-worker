# SurveyKit 审查 BUG 修复记录

范围：`codex/sprint3-report-quality-loop` 工作区，处理 [审查报告](./PROJECT_REVIEW_2026-09-05.md) 中 B1–B7。保留原有未提交改动，未部署、未执行生产迁移。

修复后的待办已重排，见 [后续迭代计划](./ITERATION_PLAN.md)。下一步为发布范围核对与跨格式统计一致性验收。

| 问题 | 修复结果 | 回归依据 |
|---|---|---|
| B1 缺失值污染 NPS/均值 | 新增共享数值转换，空值在转换前排除，真实 0 保留；全缺失 NPS 返回 null/base=0 | 同组 10+空白的总体与分组均为 100；均值为 10；10+0 的 NPS 为 0 |
| B2 截尾后假收敛 | 通过有界归一化让最终权重均值为 1 且满足上下界；重算最终残差并返回说明 | 9:1 样本、50:50 目标、最大权重 2 时，最终 80:20、残差 0.3、converged=false，最大权重确为 2 |
| B3 清洗丢弃权重 | 明确禁止直接清洗 Weighted Dataset，并提示回到 Raw/Clean 后重新加权 | 预览与执行均拒绝且不生成数据；原字节与 NPS=33.3 保持不变 |
| B4 第二张 Sheet 返回第一张 | 保留现有一文件一 Raw Dataset 契约；同 Sheet 幂等，不同 Sheet 明确拒绝；校验 store 并发去重返回值 | 顺序请求、并发返回模拟和 HTTP 400 验证；未扩展多 Sheet 数据集功能 |
| B5 项目删除漏文件 | 上传及派生文件统一解析 storage_key/storage_path；等待全部删除结束，有失败则保留元数据供重试 | 故障注入后返回失败；重试成功后逐一验证原文件/派生文件均不存在 |
| B6 加权分类分母错误 | 加权与非加权统一有效作答/分群口径；同时保留数值 0 类别 | 等权重情况下 yes=100%，有效 base 一致，空白/缺失分群被排除 |
| B7 Vite AI 代理回自身 | 直接挂载共享 AI handler，删除自代理规则；复用请求体上限配置 | 真实本地 Vite 服务：健康接口 200，缺密钥 503，非法请求 400，超大请求 413；不访问外部模型 |

主要改动位于 `lib/data-values.mjs`、`lib/data-weighting.mjs`、`lib/data-engine.mjs`、`lib/research-handler.js`、`lib/data-analysis-workflow.mjs` 和 `vite.config.js`。README 与后端说明同步更新。

新增验证：

- `tests/data-weighting.test.mjs`：27 项数值/权重测试，包含缺失值、真实 0、最终截尾范围、手算基准和非收敛说明。
- `tests/data-regressions-smoke.mjs`：跨数据存储、统计工具及本地 HTTP handler 的回归，使用临时人工样本。
- `tests/vite-ai-runtime-smoke.mjs`：真实启动 Vite 验证 AI 接口，隔离个人环境文件和模型密钥。

这些测试已接入 `npm test`。旧审查复现脚本断言的是错误行为，保留作历史证据；日常验证使用上述正式测试。

本轮验证全部完成：`npm test` 通过（5 个单元测试文件、85 项用例及完整 Node 冒烟链）；`npm run build` 通过；`npm run test:e2e` 为 20 通过、1 跳过；`git diff --check` 通过。构建保留了原有传统 script 打包警告；浏览器跳过项仍为需要显式启用的 file 协议测试。本轮没有改动 Python 代码，未重复执行上一轮已通过的 Python 测试。

验证日志：

- [Node 测试](../../outputs/surveykit-fixes-npm-test.log)
- [构建](../../outputs/surveykit-fixes-build.log)
- [浏览器测试](../../outputs/surveykit-fixes-e2e.log)

边界：修复不重写已有分析结果、证据和成果；如旧结果包含缺失值或截尾权重，应在修复版本中重新计算后核对。历史已删除项目的孤立文件未自动扫描清除。本次没有修改 Python 渲染器和数据库 schema。
